import { View } from "react-native";
import styled from "styled-components/native";

const InputContainer = styled(View)`
  row-gap: 6px;
`;

const RatingContainer = styled(View)`
  flex-direction: row;
  align-items: center;
`;

export { InputContainer, RatingContainer };
